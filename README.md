# Havas Pilot

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
# Start the main counting pipeline
python main.py

# Start the analytics dashboard
streamlit run dashboard/app.py

# Install dependencies
pip install -r requirements.txt
```
